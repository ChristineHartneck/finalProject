/*
1.schritt ohne backend: 
eigene definieren der Cards:
cards = [...]

2.schritt mit backend: 
make AJAX call 
cards = response des AJAXs

dynamisches hinzufügen der cards (ungefähr :D)
for (0 bis end) {
    $('#idDesElements').add(<li> {card} </li>);
}
*/

